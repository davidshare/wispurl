[project]
name = "wispurl-metrics"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "David Essien", email = "davidessienshare@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "prometheus-client>=0.26.0",
]

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
