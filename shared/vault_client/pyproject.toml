[project]
name = "wispurl-vault-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "David Essien", email = "davidessienshare@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "hvac>=2.4.0",
]

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "vault_client"